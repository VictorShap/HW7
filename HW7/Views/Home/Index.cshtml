@{
    ViewData["Title"] = "Home Budget Tracker";
}

<h1>Home Budget Tracker</h1>

<p>Welcome to your personal expense tracking system.</p>

<p>
    <a class="btn btn-primary" href="@Url.Action("Index", "ExpenseCategories")">
        View Expense Categories
    </a>
    <a class="btn btn-primary" href="@Url.Action("Index", "Expenses")">
        View Expenses
    </a>
    <a class="btn btn-primary" href="@Url.Action("Index", "Summaries")">
        View Stats
    </a>
</p>